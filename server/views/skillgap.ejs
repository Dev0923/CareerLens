<% const content = `
  <style>
    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(30px); }
      to { opacity: 1; transform: translateY(0); }
    }
    @keyframes float {
      0%, 100% { transform: translateY(0px) rotate(0deg); }
      50% { transform: translateY(-20px) rotate(5deg); }
    }
    @keyframes shimmer {
      0% { background-position: -1000px 0; }
      100% { background-position: 1000px 0; }
    }
    
    * { box-sizing: border-box; margin: 0; padding: 0; }
    
    body {
      background: linear-gradient(135deg, #0a0e27 0%, #1a1f3a 50%, #0a0e27 100%) !important;
      margin: 0 !important;
      padding: 0 !important;
      min-height: 100vh;
      position: relative;
      overflow-x: hidden;
    }
    
    /* Animated Background Shapes */
    .animated-bg {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      pointer-events: none;
      z-index: 0;
    }
    
    .bg-shape {
      position: absolute;
      border-radius: 50%;
      filter: blur(80px);
      opacity: 0.3;
      animation: float 20s ease-in-out infinite;
    }
    
    .shape-1 {
      width: 500px;
      height: 500px;
      background: radial-gradient(circle, rgba(0, 180, 168, 0.4) 0%, transparent 70%);
      top: -200px;
      right: -200px;
      animation-delay: 0s;
    }
    
    .shape-2 {
      width: 400px;
      height: 400px;
      background: radial-gradient(circle, rgba(29, 181, 132, 0.3) 0%, transparent 70%);
      bottom: -150px;
      left: -150px;
      animation-delay: 7s;
    }
    
    .shape-3 {
      width: 350px;
      height: 350px;
      background: radial-gradient(circle, rgba(0, 180, 168, 0.25) 0%, transparent 70%);
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      animation-delay: 14s;
    }
    
    .skill-gap-container {
      max-width: 1400px;
      margin: 0 auto;
      padding: 40px 60px;
      position: relative;
      z-index: 1;
      min-height: 100vh;
    }
    
    .skill-gap-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 40px;
      padding-bottom: 20px;
      border-bottom: 2px solid rgba(255, 255, 255, 0.1);
      animation: fadeInUp 0.6s ease;
    }
    
    .skill-gap-title {
      font-size: 36px;
      margin: 0;
      background: linear-gradient(135deg, #00B4A8 0%, #1DB584 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      font-weight: 900;
      display: flex;
      align-items: center;
      gap: 12px;
    }
    
    .skill-gap-subtitle {
      margin: 8px 0 0 0;
      color: rgba(255, 255, 255, 0.7);
      font-size: 14px;
    }
    
    .back-button {
      background: rgba(255, 255, 255, 0.08);
      border: 1px solid rgba(255, 255, 255, 0.15);
      padding: 12px 24px;
      border-radius: 10px;
      font-size: 15px;
      cursor: pointer;
      font-weight: 600;
      color: white;
      transition: all 0.3s;
      text-decoration: none;
      display: inline-block;
      backdrop-filter: blur(10px);
    }
    
    .back-button:hover {
      background: rgba(255, 255, 255, 0.12);
      transform: translateY(-2px);
      box-shadow: 0 8px 24px rgba(0, 180, 168, 0.3);
    }
    
    .guide-section {
      background: rgba(255, 255, 255, 0.05);
      backdrop-filter: blur(20px);
      border-radius: 24px;
      padding: 48px;
      margin-bottom: 40px;
      box-shadow: 0 8px 32px rgba(0, 180, 168, 0.2), inset 0 1px 1px rgba(255, 255, 255, 0.1);
      border: 1px solid rgba(255, 255, 255, 0.1);
      position: relative;
      animation: fadeInUp 0.8s ease;
    }
    
    .guide-section::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 2px;
      background: linear-gradient(90deg, transparent, #00B4A8, #1DB584, transparent);
    }
    
    .guide-title {
      margin: 0 0 36px;
      background: linear-gradient(135deg, #00B4A8 0%, #1DB584 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      font-size: 28px;
      text-align: center;
      font-weight: 800;
      letter-spacing: -0.5px;
    }
    
    .step-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 24px;
      margin-bottom: 24px;
    }
    
    .step-card {
      padding: 28px;
      background: rgba(255, 255, 255, 0.05);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(0, 180, 168, 0.3);
      border-radius: 16px;
      text-align: center;
      transition: all 0.3s ease;
      animation: fadeInUp 0.6s ease backwards;
    }
    
    .step-card:hover {
      transform: translateY(-8px);
      background: rgba(255, 255, 255, 0.08);
      box-shadow: 0 12px 40px rgba(0, 180, 168, 0.3);
      border-color: rgba(0, 180, 168, 0.6);
    }
    
    .step-number {
      width: 48px;
      height: 48px;
      background: linear-gradient(135deg, #00B4A8 0%, #1DB584 100%);
      color: white;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 20px;
      font-weight: 700;
      margin: 0 auto 20px;
      box-shadow: 0 4px 16px rgba(0, 180, 168, 0.4);
    }
    
    .step-title {
      margin: 0 0 12px;
      color: white;
      font-size: 18px;
      font-weight: 600;
    }
    
    .step-desc {
      margin: 0;
      color: rgba(255, 255, 255, 0.7);
      font-size: 14px;
      line-height: 1.6;
    }
    
    .upload-section {
      background: rgba(255, 255, 255, 0.05);
      backdrop-filter: blur(20px);
      border-radius: 24px;
      padding: 48px;
      box-shadow: 0 8px 32px rgba(0, 180, 168, 0.2), inset 0 1px 1px rgba(255, 255, 255, 0.1);
      border: 1px solid rgba(255, 255, 255, 0.1);
      animation: fadeInUp 1s ease;
    }
    
    .upload-header {
      display: flex;
      align-items: center;
      gap: 20px;
      margin-bottom: 32px;
    }
    
    .upload-icon-large {
      font-size: 64px;
    }
    
    .upload-title {
      font-size: 24px;
      font-weight: 700;
      color: white;
      margin: 0 0 8px;
    }
    
    .upload-subtitle {
      color: rgba(255, 255, 255, 0.7);
      font-size: 14px;
      margin: 0;
    }
    
    .upload-zone {
      background: rgba(16, 24, 48, 0.8);
      border: 2px dashed rgba(0, 180, 168, 0.4);
      border-radius: 20px;
      padding: 60px 40px;
      text-align: center;
      cursor: pointer;
      transition: all 0.3s ease;
      position: relative;
      overflow: hidden;
    }
    
    .upload-zone::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(0, 180, 168, 0.1), transparent);
      transition: left 0.5s;
    }
    
    .upload-zone:hover::before {
      left: 100%;
    }
    
    .upload-zone:hover {
      border-color: #00B4A8;
      background: rgba(16, 24, 48, 0.95);
      box-shadow: 0 8px 32px rgba(0, 180, 168, 0.3);
    }
    
    .upload-placeholder-icon {
      font-size: 64px;
      margin-bottom: 20px;
      animation: float 3s ease-in-out infinite;
    }
    
    .upload-text {
      margin: 0 0 8px;
      font-size: 18px;
      font-weight: 600;
      color: white;
    }
    
    .upload-subtext {
      margin: 0 0 24px;
      color: rgba(255, 255, 255, 0.6);
      font-size: 14px;
    }
    
    .browse-btn {
      margin-top: 20px;
      background: linear-gradient(135deg, #00B4A8 0%, #1DB584 100%);
      color: white;
      padding: 14px 36px;
      border: none;
      border-radius: 10px;
      font-weight: 700;
      font-size: 15px;
      cursor: pointer;
      transition: all 0.3s;
      box-shadow: 0 6px 20px rgba(0, 180, 168, 0.4);
      letter-spacing: 0.3px;
    }
    
    .browse-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 30px rgba(0, 180, 168, 0.5);
    }
    
    @media (max-width: 968px) {
      .step-grid {
        grid-template-columns: 1fr;
      }
      .skill-gap-container {
        padding: 30px 20px;
      }
      .guide-section,
      .upload-section {
        padding: 32px 24px;
      }
      .skill-gap-header {
        flex-direction: column;
        align-items: flex-start;
        gap: 16px;
      }
    }
  </style>

  <!-- Animated Background -->
  <div class="animated-bg">
    <div class="bg-shape shape-1"></div>
    <div class="bg-shape shape-2"></div>
    <div class="bg-shape shape-3"></div>
  </div>

  <div class="skill-gap-container">
    <!-- Header -->
    <div class="skill-gap-header">
      <div>
        <h1 class="skill-gap-title">
          🎯 Skill Gap Analyzer
        </h1>
        <p class="skill-gap-subtitle">
          Discover your skill gaps and salary potential
        </p>
      </div>
      <a href="/" class="back-button">
        ← Back
      </a>
    </div>

    <!-- Guide Section -->
    <div class="guide-section">
      <h2 class="guide-title">
        📚 How to Get Started
      </h2>

      <!-- Three Steps in First Row -->
      <div class="step-grid">
        <div class="step-card" style="animation-delay: 0s;">
          <div class="step-number">1</div>
          <h3 class="step-title">Upload Resume</h3>
          <p class="step-desc">Select your PDF resume - it will be auto-extracted</p>
        </div>

        <div class="step-card" style="animation-delay: 0.1s;">
          <div class="step-number">2</div>
          <h3 class="step-title">Add Job Description</h3>
          <p class="step-desc">Paste the job posting you're targeting</p>
        </div>

        <div class="step-card" style="animation-delay: 0.2s;">
          <div class="step-number">3</div>
          <h3 class="step-title">Get Analysis</h3>
          <p class="step-desc">Click HR Review or ATS Match Score button</p>
        </div>
      </div>
    </div>

    <!-- Upload Section -->
    <div class="upload-section">
      <div class="upload-header">
        <div class="upload-icon-large">📁</div>
        <div>
          <h3 class="upload-title">Upload files</h3>
          <p class="upload-subtitle">Select and upload the files of your choice</p>
        </div>
      </div>
      
      <div class="upload-zone" onclick="document.getElementById('resume-upload').click()">
        <div class="upload-placeholder-icon">📄</div>
        <div class="upload-text">Choose a file or drag & drop it here</div>
        <p class="upload-subtext">PDF format, up to 10MB</p>
        <input
          type="file"
          accept=".pdf"
          id="resume-upload"
          style="display: none;"
        />
        <button class="browse-btn" type="button">
          Browse File
        </button>
      </div>
    </div>
  </div>
`; %>
<%- include('layout', { title: 'Skill Gap Analyzer', body: content }) %>
