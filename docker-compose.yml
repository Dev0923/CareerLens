version: '3.9'
services:
  app:
    build: .
    ports:
      - "4000:4000"
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
      - PORT=4000
    volumes:
      - ./config.yaml:/app/config.yaml
    restart: unless-stopped
